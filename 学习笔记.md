制造智能技术课程设计——学习笔记
一、AI工具学习（Vibe Coding）
  Vibe Coding是一种“AI配对编程”方法，核心在于通过高质量的提示词和有效的上下文管理，让AI成为高效的编程助手，而非简单的代码生成器。我系统学习了以下四个核心模块：
1.1提示词工程
  提示词工程是Vibe Coding的基础，目的是通过结构化、清晰的指令引导AI生成高质量代码。关键技巧包括：
1.1.1 任务拆解：将复杂功能拆解为多个独立可验证的子任务，逐个实现。例如开发缺陷检测系统时，拆解为“数据加载与预处理 → CNN模型定义 → 训练循环 → 模型评估 → API接口封装”五个阶段，每个阶段单独编写提示词，降低AI出错概率。
1.1.2 约束说明：在提示词中明确指定技术栈版本、输入输出格式、命名规范等硬性约束。例如：“使用PyTorch 2.0+，输入shape为(batch, 3, 224, 224)，输出为6类概率分布，使用CrossEntropyLoss损失函数”。
1.1.3 验收标准：在提示词末尾附带测试用例或预期行为作为验收条件，方便快速验证AI生成代码的正确性。
1.2上下文工程
  上下文工程指让AI充分理解项目现状，避免每次都“从头开始”，减少代码风格不一致和重复劳动：
1.2.1 创建项目目录结构，将项目文件按功能模块进行分类，并创建相应的目录和文件。
1.2.2 建立项目规则文件（如`.cursorrules`），统一代码风格、命名规范、技术栈版本。
1.2.3 修改已有代码时，先让AI阅读并解释相关代码段，确认理解后再提出修改需求。
1.2.4 避免使用“重新生成全部代码”的指令，而是针对具体函数或模块提出修改。
1.3规格驱动开发
  规格驱动开发强调“先写规格，再写代码”的开发流程：
1.3.1 编写需求文档：明确功能描述、输入输出、技术约束、验收标准。    
1.3.2 AI制定计划：让AI根据需求文档生成开发任务清单。
1.3.3 分步实现：AI按计划逐模块生成代码。
1.3.4 逐步验收：每完成一步，人工对照验收标准进行验证。
该方法能有效避免AI生成“看起来合理但实际上跑不通”的代码，同时便于开发过程的管理和回溯。
1.4调试与人工接管策略
当AI生成的代码运行出错时，采用以下应对策略：
1.4.1 回滚：使用`git reset --hard`或`git revert`回退到最近一个稳定版本。
1.4.2 缩小改动范围：将问题定位到具体函数或代码块，只提交针对性修复，而非整体重写。
1.4.3 最小复现：构造最小化测试用例，帮助AI快速定位问题根因。
1.4.4 人工接管：当AI反复修复无效时，手动阅读并修改关键代码段，而非盲目依赖AI继续尝试。
二、拟选用的Harness和模型
2.1 在Harness工具的选择上，我计划选用Trae。这主要是因为它目前对个人开发者完全免费，能够零成本使用；同时它原生支持多文件上下文编辑，在涉及跨文件修改时效率很高。此外，Trae的中文交互对国内开发者非常友好，能够降低沟通成本，并且它对PyTorch和FastAPI这两个本次开发核心框架的代码生成质量较高，能够有效提升开发效率。
2.2 在大语言模型方面，我计划选用Doubao-Seed-2.0-mini。该模型代码生成能力较强，且拥有较大的上下文窗口，能够完整分析项目文件，避免因上下文截断导致的代码不一致问题。同时，它在Python深度学习相关代码生成上表现较好，能够更好地支持本次课程设计中的模型训练与后端服务开发任务。
2.3 在实际开发中，我将结合提示词工程（任务拆解、约束说明、验收标准）使用上述工具，并保留完整的Prompt日志作为过程档案，以满足任务书对过程考核的要求。
三、Git原理与功能
3.1 Git核心概念：Git是一个分布式版本控制系统，用于追踪文件变更历史，支持多人协作开发和多版本管理。其核心工作区域分为：
3.2 Git的版本管理机制围绕四个核心区域展开，形成了一条从本地修改到云端存储的完整数据流。工作区是开发者直接编辑文件的本地目录，所有代码修改都在此进行；当修改完成后，通过git add命令将变更提交到暂存区，这是一个临时存储区域，允许开发者精细地选择哪些修改需要纳入版本控制；随后执行git commit将暂存区内容固化为本地仓库中的一个版本快照，形成永久且可追溯的历史记录；最后通过git push将本地仓库的版本同步到远程仓库（托管在GitHub、Gitee等平台），实现代码的云端备份与团队协作共享。四个区域层层递进，既保证了本地开发的灵活性，又确保了版本变更的完整性和可追溯性。
3.3 Git分支管理
分支是Git实现多人协作和并行开发的核心机制。本次开发采用Git Flow简化版流程：
Git的分支管理机制为团队协作和版本控制提供了清晰的流程保障。在这个体系中，main分支作为主分支始终保持稳定且可运行的状态，代表随时可以交付的版本；dev分支则是日常开发的集成分支，用于汇总各功能模块的阶段性成果；而feature/*分支是为每个新功能独立创建的开发分支，开发者在此完成功能开发后，再通过git merge合并回dev分支。各分支之间通过git merge命令进行合并，既保证了并行开发的独立性，又确保了代码变更的有序集成和全程可追溯。
3.4 本次课程设计常用Git命令
Git的常用命令覆盖了从仓库初始化到远程协作的完整开发流程。在项目启动时，通过git init在本地文件夹创建新仓库，并用git remote add origin <URL>将其与远程仓库关联；日常开发中，使用git add <文件名>或git add .将修改添加到暂存区，再通过git commit -m "提交说明"提交为本地版本；完成本地提交后，首次推送使用git push -u origin main建立本地与远程分支的关联，后续则直接使用git push同步到远程，而git pull则用于拉取远程的最新变更。在开发过程中，git status可随时查看文件变更状态，git log --oneline查看简洁的提交历史，git branch列出所有分支，git checkout -b <分支名>用于创建并切换到新分支。当需要撤销或回退时，git reset --hard <commit-hash>可回滚到指定版本（丢弃后续修改），而git revert <commit-hash>则以新增提交的方式撤销指定提交，更适合协作场景；分支合并通过git merge <分支名>将指定分支的变更集成到当前分支。这些命令相互配合，构成了Git版本控制的完整操作体系。
3.5 本课程的Git使用计划
按照任务书要求，坚持“小步提交、有意义的commit信息”原则：
在本次课程设计的Git使用计划中，我坚持“小步提交、有意义的commit信息”原则，即每完成一个功能点（如数据加载模块、模型训练脚本、API接口封装等）即进行一次commit，确保每次提交的粒度适中、变更聚焦。同时，commit信息采用规范化前缀：`feat:`表示新功能，`fix:`表示修复Bug，`docs:`表示文档更新，`test:`表示测试用例，`refactor:`表示代码重构。例如，提交信息可写为`feat: 完成ResNet18模型定义和训练脚本`、`fix: 修复数据加载路径错误`或`docs: 更新README运行说明`。这种规范化的提交习惯有助于清晰追踪开发进度，也便于答辩时展示过程档案。
四、选题调研
4.1 调研过程
通过查阅公开工业数据集和相关文献，共调研了以下三个候选方向：
在选题调研阶段，我共考察了三个候选方向。第一个是数控刀具剩余寿命预测，采用PHM 2010数据集（含多通道传感器时序数据），技术路线上拟用LSTM进行时序预测、XGBoost作为对比基线、遗传算法用于超参数调优，该方向业务感强，但时间序列数据的预处理和特征工程较为复杂。第二个是热轧带钢表面缺陷检测，采用东北大学NEU数据集（图像数据），技术路线为CNN图像分类加SVM对比，并集成后端系统，该方案数据直观、代码模板成熟、视觉展示效果好，适合在有限周期内快速完成开发。第三个是车间生产排程优化，采用Taillard基准实例（需自行仿真生成数据），技术路线为遗传算法求解调度问题结合神经网络进行工时预测，该方向算法有挑战性，但数据需自行生成，不确定性较高。经综合比较数据可获得性、技术覆盖度和开发周期，最终确定选题为热轧带钢表面缺陷检测。
4.2 最终选题
经综合比较数据可获得性、技术覆盖度和开发周期，最终确定选题为：
《基于深度学习的热轧带钢表面缺陷检测系统》
4.3 选题背景与意义
热轧带钢是钢铁工业的重要产品，其表面质量直接影响后续加工性能。在生产过程中，由于轧辊磨损、工艺参数波动、冷却不均等原因，钢材表面可能出现多种典型缺陷。传统的人工目视检测方式存在效率低、标准不统、漏检率高等问题，难以适应高速自动化生产线的质量控制需求。
因此，开发基于深度学习的钢材表面缺陷自动检测系统，实现从人工目检到智能检测的升级，对于提升产品质量控制水平、降低生产成本、减少废品率具有重要的实际工程意义。
4.4 数据来源
本项目采用东北大学发布的NEU表面缺陷数据库（NEU-CLS），该数据集是工业缺陷检测领域的标准基准数据集，包含6类典型表面缺陷共1800张灰度图像，每类300张，图像尺寸为200×200像素。数据已公开发布，可通过学术渠道或GitHub直接获取。
六类缺陷具体为：
NEU表面缺陷数据库涵盖了热轧带钢生产中常见的六类典型缺陷。其中，裂纹（Cr）表现为表面呈现的网状或线状开裂；夹杂物（In）是表面嵌入的非金属杂质颗粒；斑块（Pa）呈现为块状或片状的颜色斑痕；麻点（PS）表现为表面密布的微小凹坑或蚀点；氧化皮（RS）是轧制过程中压入表面的氧化铁皮；划痕（Sc）则是表面存在的线状机械划伤。这六类缺陷每类包含300张灰度图像，共同构成了该数据集的1800张样本总量，为后续缺陷检测模型的训练与评估提供了均衡且标准化的数据基础。
4.5 三个技术方向映射
本系统设计了三个技术方向，分别对应课程所学内容并承担明确的系统职能。第一个方向是计算机视觉（深度学习），对应课程中的深度学习与卷积神经网络章节，具体使用PyTorch构建ResNet18网络，在NEU数据集上训练缺陷6分类模型，作为系统的核心识别引擎，负责对上传图像进行自动缺陷分类。第二个方向是机器学习（传统分类器），对应机器学习基础与特征工程章节，通过提取图像的HOG或LBP纹理特征，训练SVM分类器作为对比基线，用于验证深度学习方法的优越性。第三个方向是后端服务与数据库集成，对应系统集成与架构设计章节，使用FastAPI封装模型预测接口，MySQL存储检测记录，配合Streamlit前端构成完整的B/S架构Web应用。三个方向层层递进，既实现了核心算法的开发与对比验证，也构建了从前端交互到后端服务的完整业务闭环。
五、系统架构初步设想
本项目采用B/S架构，各层职责清晰：
5.1 本系统采用B/S架构，自下而上分为四个层次。前端层基于Streamlit框架实现，提供用户上传缺陷图像、查看检测结果和历史记录的交互界面；后端层使用FastAPI构建RESTful API服务，承载业务逻辑、模型加载与推理调度，作为前后端通信的桥梁；算法层包含两个核心模型——基于PyTorch实现的ResNet18作为主分类模型，以及基于scikit-learn实现的SVM作为对比基线；数据层采用MySQL存储检测记录，包括图片路径、预测类别、置信度和检测时间等信息。
5.2 系统完整业务流程为：用户在前端界面上传缺陷图像，后端服务接收图像后调用算法层的分类模型进行推理预测，将预测结果（缺陷类别与置信度）存入数据库，最后将结果返回前端展示给用户，形成从数据输入到结果反馈的完整业务闭环。
六、可行性分析
6.1 NEU数据集公开可获取，代码参考资源丰富，CNN图像分类技术路线成熟，10天开发周期内可完成从数据处理、模型训练到系统集成的完整开发流程。


&#x20;制造智能技术课程设计——学习笔记



&#x20;一、AI工具学习（Vibe Coding）



Vibe Coding是一种“AI配对编程”方法，核心在于通过高质量的提示词和有效的上下文管理，让AI成为高效的编程助手，而非简单的代码生成器。我系统学习了以下四个核心模块：



&#x20;1. 提示词工程



提示词工程是Vibe Coding的基础，目的是通过结构化、清晰的指令引导AI生成高质量代码。关键技巧包括：



\- 任务拆解：将复杂功能拆解为多个独立可验证的子任务，逐个实现。例如开发缺陷检测系统时，拆解为“数据加载与预处理 → CNN模型定义 → 训练循环 → 模型评估 → API接口封装”五个阶段，每个阶段单独编写提示词，降低AI出错概率。



\- 约束说明：在提示词中明确指定技术栈版本、输入输出格式、命名规范等硬性约束。例如：“使用PyTorch 2.0+，输入shape为(batch, 3, 224, 224)，输出为6类概率分布，使用CrossEntropyLoss损失函数”。



\- 验收标准：在提示词末尾附带测试用例或预期行为作为验收条件，方便快速验证AI生成代码的正确性。



&#x20;2. 上下文工程



上下文工程指让AI充分理解项目现状，避免每次都“从头开始”，减少代码风格不一致和重复劳动：



\- 建立项目规则文件（如`.cursorrules`），统一代码风格、命名规范、技术栈版本

\- 修改已有代码时，先让AI阅读并解释相关代码段，确认理解后再提出修改需求

\- 避免使用“重新生成全部代码”的指令，而是针对具体函数或模块提出修改



&#x20;3. 规格驱动开发



规格驱动开发强调“先写规格，再写代码”的开发流程：



1\. 编写需求文档：明确功能描述、输入输出、技术约束、验收标准

2\. AI制定计划：让AI根据需求文档生成开发任务清单

3\. 分步实现：AI按计划逐模块生成代码

4\. 逐步验收：每完成一步，人工对照验收标准进行验证



该方法能有效避免AI生成“看起来合理但实际上跑不通”的代码，同时便于开发过程的管理和回溯。



&#x20;4. 调试与人工接管策略



当AI生成的代码运行出错时，采用以下应对策略：



\- 回滚：使用`git reset --hard`或`git revert`回退到最近一个稳定版本

\- 缩小改动范围：将问题定位到具体函数或代码块，只提交针对性修复，而非整体重写

\- 最小复现：构造最小化测试用例，帮助AI快速定位问题根因

\- 人工接管：当AI反复修复无效时，手动阅读并修改关键代码段，而非盲目依赖AI继续尝试





&#x20;二、拟选用的Harness和模型



| 类别 | 选用工具 | 理由 |

| :--- | :--- | :--- |

| Harness（AI编程助手） | Trae | 免费、支持多文件上下文编辑、中文交互友好、对PyTorch和FastAPI代码生成质量较高 |

| 大语言模型 | Deepseek-v4 | 代码生成能力强、上下文窗口大（支持完整项目文件分析）、擅长Python深度学习相关代码 |



开发过程中拟结合多种提示词策略（任务拆解、约束说明、验收标准等）使用上述工具完成系统开发，并保留完整的Prompt日志作为过程档案。





&#x20;三、Git原理与功能



&#x20;1. Git核心概念



Git是一个分布式版本控制系统，用于追踪文件变更历史，支持多人协作开发和多版本管理。其核心工作区域分为：



| 区域 | 说明 |

| :--- | :--- |

| 工作区（Working Directory） | 本地编辑文件的目录，即项目文件夹 |

| 暂存区（Staging Area） | 通过`git add`将修改暂存于此，待统一提交 |

| 本地仓库（Local Repository） | 通过`git commit`将暂存区内容保存为版本快照 |

| 远程仓库（Remote Repository） | 托管在GitHub/Gitee等平台的中央仓库，用于备份和协作 |



&#x20;2. Git分支管理



分支是Git实现多人协作和并行开发的核心机制。本次开发采用Git Flow简化版流程：



\- `main`分支：主分支，始终保持稳定可运行状态

\- `dev`分支：开发分支，用于日常功能集成

\- `feature/\*`分支：功能分支，每个新功能独立开发，完成后合并回dev



各分支之间通过`git merge`合并，确保开发过程有序、可追溯。



&#x20;3. 本次课程设计常用Git命令



| 命令 | 作用 |

| :--- | :--- |

| `git init` | 在当前文件夹初始化一个新的Git仓库 |

| `git add <文件名>` | 将指定文件添加到暂存区 |

| `git add .` | 将所有变更文件添加到暂存区 |

| `git commit -m "提交说明"` | 将暂存区内容提交为本地版本 |

| `git remote add origin <URL>` | 关联本地仓库与远程仓库 |

| `git push -u origin main` | 首次推送并建立本地与远程分支的关联 |

| `git push` | 后续推送本地提交到远程仓库 |

| `git pull` | 拉取远程仓库的最新变更到本地 |

| `git status` | 查看当前文件变更状态 |

| `git log --oneline` | 查看简洁版提交历史记录 |

| `git branch` | 查看当前分支列表 |

| `git checkout -b <分支名>` | 创建并切换到一个新分支 |

| `git reset --hard <commit-hash>` | 回滚到指定历史版本（丢弃后续所有修改） |

| `git revert <commit-hash>` | 撤销指定提交（保留历史记录，适合协作场景） |

| `git merge <分支名>` | 将指定分支合并到当前分支 |



&#x20;4. 本课程的Git使用计划



按照任务书要求，坚持“小步提交、有意义的commit信息”原则：



\- 每完成一个功能点（如数据加载、模型训练、API接口）即进行一次commit

\- commit信息使用规范化前缀：`feat:`（新功能）、`fix:`（修复Bug）、`docs:`（文档更新）、`test:`（测试用例）、`refactor:`（代码重构）

\- 示例：`feat: 完成ResNet18模型定义和训练脚本`、`fix: 修复数据加载路径错误`、`docs: 更新README运行说明`





&#x20;四、选题调研



&#x20;1. 调研过程



通过查阅公开工业数据集和相关文献，共调研了以下三个候选方向：



| 候选方向 | 数据来源 | 技术路线 | 优缺点 |

| :--- | :--- | :--- | :--- |

| 数控刀具剩余寿命预测 | PHM 2010数据集（多通道传感器时序数据） | LSTM时序预测 + XGBoost对比 + 遗传算法调参 | 业务感强，但时间序列数据预处理和特征工程较复杂 |

| 热轧带钢表面缺陷检测 | 东北大学NEU数据集（图像数据） | CNN图像分类 + SVM对比 + 后端系统集成 | 数据直观、代码模板成熟、视觉展示效果好、适合快速开发 |

| 车间生产排程优化 | Taillard基准实例（需自行仿真生成数据） | 遗传算法求解调度问题 + 神经网络工时预测 | 算法有挑战性，但数据需自行生成，不确定性较高 |



&#x20;2. 最终选题



经综合比较数据可获得性、技术覆盖度和开发周期，最终确定选题为：



《基于深度学习的热轧带钢表面缺陷检测系统》



&#x20;3. 选题背景与意义



热轧带钢是钢铁工业的重要产品，其表面质量直接影响后续加工性能。在生产过程中，由于轧辊磨损、工艺参数波动、冷却不均等原因，钢材表面可能出现多种典型缺陷。传统的人工目视检测方式存在效率低、标准不统一、漏检率高等问题，难以适应高速自动化生产线的质量控制需求。



因此，开发基于深度学习的钢材表面缺陷自动检测系统，实现从人工目检到智能检测的升级，对于提升产品质量控制水平、降低生产成本、减少废品率具有重要的实际工程意义。



&#x20;4. 数据来源



本项目采用东北大学发布的NEU表面缺陷数据库（NEU-CLS），该数据集是工业缺陷检测领域的标准基准数据集，包含6类典型表面缺陷共1800张灰度图像，每类300张，图像尺寸为200×200像素。数据已公开发布，可通过学术渠道或GitHub直接获取。



六类缺陷具体为：



| 缺陷类型 | 英文缩写 | 特征描述 |

| :--- | :--- | :--- |

| 裂纹 | Cr | 表面呈现的网状或线状开裂 |

| 夹杂物 | In | 表面嵌入的非金属杂质颗粒 |

| 斑块 | Pa | 表面呈现的块状或片状颜色斑痕 |

| 麻点 | PS | 表面密布的微小凹坑或蚀点 |

| 氧化皮 | RS | 轧制过程中压入表面的氧化铁皮 |

| 划痕 | Sc | 表面存在的线状机械划伤 |



&#x20;5. 三个技术方向映射



| 序号 | 技术方向 | 对应课程内容 | 在系统中的具体作用 |

| :--- | :--- | :--- | :--- |

| 1 | 计算机视觉（深度学习） | 深度学习/卷积神经网络 | 使用PyTorch构建ResNet18网络，在NEU数据集上训练缺陷6分类模型，作为系统的核心识别引擎 |

| 2 | 机器学习（传统分类器） | 机器学习基础/特征工程 | 提取图像的HOG或LBP纹理特征，训练SVM分类器作为对比基线，验证深度学习方法的优越性 |

| 3 | 后端服务与数据库集成 | 系统集成与架构设计 | 使用FastAPI封装模型预测接口，MySQL存储检测记录，配合Streamlit前端构成完整B/S架构Web应用 |





&#x20;五、系统架构初步设想



本项目采用B/S架构，各层职责清晰：



\- 前端层：Streamlit框架实现，用户可上传缺陷图像、查看检测结果和历史记录

\- 后端层：FastAPI提供RESTful API，承载业务逻辑、模型加载与推理调度

\- 算法层：PyTorch实现ResNet18分类模型 + scikit-learn实现SVM对比模型

\- 数据层：MySQL存储检测记录（图片路径、预测类别、置信度、检测时间等）



用户完整业务流程：前端上传图片 → 后端接收并调用算法模型预测 → 结果存入数据库 → 前端展示预测类别与置信度。



&#x20;六、可行性分析



NEU数据集公开可获取，代码参考资源丰富，CNN图像分类技术路线成熟，10天开发周期内可完成从数据处理、模型训练到系统集成的完整开发流程。


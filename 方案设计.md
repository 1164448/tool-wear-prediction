# 制造智能技术课程设计——方案设计

## 一、功能需求分析

### 1.1 核心功能需求

| 功能模块 | 需求描述 | 优先级 |
| :--- | :--- | :--- |
| 图像上传 | 用户通过Web界面上传钢材表面图像，支持jpg/jpeg/png/bmp格式 | 高 |
| 智能检测 | 系统自动识别图像所属的缺陷类别，并输出置信度 | 高 |
| 结果展示 | 清晰展示缺陷类型和置信度，提供友好的用户反馈 | 高 |

### 1.2 用户角色

- **质检员**：上传钢材表面图像，获取缺陷检测结果

### 1.3 非功能需求

- 单张图片检测响应时间 ≤ 3秒
- 模型在验证集上的准确率 ≥ 85%
- 界面简洁，操作流程不超过三步

## 二、方案论证

### 2.1 技术方案对比

| 方案 | 优点 | 缺点 | 选择 |
| :--- | :--- | :--- | :--- |
| 传统机器学习（SVM+HOG） | 计算资源要求低、可解释性强 | 特征工程依赖人工、准确率有限 | 作为对比基线 |
| 深度学习（CNN） | 端到端学习、准确率高 | 需要较多数据和计算资源 | ✅ 主方案 |

### 2.2 模型选型

选择ResNet18作为骨干网络，主要原因：
1. 在ImageNet上预训练，迁移学习效果好；
2. 参数量适中（约11M），适合CPU推理；
3. 在NEU数据集上有成熟的应用案例。

### 2.3 开发工具选型

本课题采用Vibe Coding方法完成开发：

- **AI编程助手**：选用 **Deepseek**，该模型在代码生成、Bug修复和逻辑推理方面表现优异，对PyTorch和FastAPI等框架的代码生成质量较高，能够有效辅助完成从模型训练到Web应用开发的全流程编码任务。在实际开发中，通过任务拆解、约束说明、验收标准等提示词策略，引导Deepseek分模块生成高质量代码，并保留完整的Prompt日志作为过程档案。
- **Harness工具**：配合使用 **Trae** 作为IDE插件，提供实时代码续写、智能补全和项目构建支持，与Deepseek大模型协同完成开发工作。Trae的中文交互界面友好，对PyTorch和FastAPI等框架的代码生成质量较高，能够有效提升开发效率。
- **大语言模型**：选用**Doubao-Seed-2.0-mini**，该模型是豆包大模型2.0系列中的轻量版本，以速度和成本优先，代码生成能力较强，适合深度学习相关代码的开发任务。

### 2.4 架构选型

采用B/S架构，理由：
1. 无需安装客户端，使用浏览器即可访问；
2. 便于部署和更新；
3. 符合任务书对B/S架构的要求。

## 三、方案规划

### 3.1 技术栈

| 层级 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| 前端 | Streamlit | 纯Python开发，快速构建Web界面 |
| 后端 | FastAPI | 高性能异步框架，自动生成API文档 |
| 算法 | PyTorch + scikit-learn | 深度学习框架 + 机器学习库 |
| 数据库 | MySQL | 存储检测记录 |
| 版本控制 | Git + GitHub | 全程管理代码版本 |
| Harness工具 | Trae | AI辅助编程 |
| 大语言模型 | Doubao-Seed-2.0-mini | 代码生成与调试 |

### 3.2 数据集来源

**东北大学NEU表面缺陷数据库（NEU-CLS）**

- 发布机构：东北大学；
- 包含六类缺陷：裂纹（Cr）、夹杂物（In）、斑块（Pa）、麻点（PS）、氧化皮（RS）、划痕（Sc）；
- 每类300张灰度图像，共1800张；
- 图像尺寸：200×200像素；
- 获取方式：Kaggle公开下载。

### 3.3 前端UI方案

使用Streamlit构建单页应用，包含：
- 标题区：显示系统名称和描述；
- 图片上传区：支持点击上传；
- 结果展示区：缺陷类型 + 置信度百分比；
- 底部信息：模型信息、数据集信息。

### 3.4 后端算法方案

**算法一：深度学习（ResNet18）**
- 使用torchvision.models.resnet18预训练权重；
- 替换全连接层为六分类输出；
- 冻结除最后一层外的所有参数；
- 优化器：Adam，学习率0.001；
- 训练轮数：30 epochs；
- 预期准确率：≥90%。

**算法二：机器学习（SVM对比）**
- 提取HOG（方向梯度直方图）特征；
- 训练RBF核SVM分类器；
- 与ResNet18结果进行对比。

**算法三：后端服务与数据库**
- FastAPI提供 `/predict` 接口；
- 接收图片 → 预处理 → 模型推理 → 返回结果；
- MySQL存储检测记录。

## 四、技术路线

### 4.1 总体流程
- 数据准备 → 模型训练 → 模型评估 → 后端开发 → 前端开发 → 集成测试 → 部署演示

### 4.2 详细步骤

1. **数据准备**：下载NEU数据集，按8:2划分训练/验证集；
2. **模型训练**：使用ResNet18迁移学习，训练六分类模型；
3. **后端开发**：FastAPI实现 `/predict` 接口，加载模型进行推理；
4. **前端开发**：Streamlit实现图片上传和结果展示界面；
5. **集成测试**：前后端联调，完整业务流程测试；
6. **成果提交**：撰写报告、录制视频、准备答辩。

## 五、计划安排

| 阶段 | 时间 | 任务内容 |
| :--- | :--- | :--- |
| 环境搭建与学习 | D1-D2 | 安装Python、配置虚拟环境、学习Vibe Coding方法 |
| 选题与方案设计 | D3-D5 | 确定选题、撰写需求规格、设计系统架构 |
| 数据准备 | D6 | 下载数据集、数据清洗与统计分析 |
| 模型训练 | D7-D8 | ResNet18迁移学习训练、模型保存 |
| 系统开发 | D7-D8 | FastAPI后端开发、Streamlit前端开发 |
| 集成调试 | D9 | 前后端联调、完整业务流程测试 |
| 报告与答辩 | D9-D10 | 撰写设计报告、制作PPT、录制视频 |

## 六、参考文献

[1] Song K, Yan Y. NEU surface defect database. Northeastern University, 2013.
[2] He K, Zhang X, Ren S, et al. Deep Residual Learning for Image Recognition[C]. CVPR, 2016.
[3] 张智海等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.
[4] 东北大学NEU表面缺陷数据库. http://faculty.neu.edu.cn/me/songkc/